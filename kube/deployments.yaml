apiVersion: v1
kind: Pod
metadata:
 name: mypod
spec:
  containers:
    - image: docker.io/kundankkkrishna/nodeapp:latest
      name: nodeapp
      ports:
        - containerPort: 8081
      resources:
        limits:
          cpu: 100m
          memory: 128Mi
        requests:
          cpu: 100m
          memory: 128Mi
          
---
apiVersion: apps/v1
kind: Deployment
metadata:
    name: nodeapp
spec:
    selector:
        matchLabels:
            app: nodeapp
    replicas: 2
    template:
        metadata:
            labels:
                app: nodeapp
        spec:
            containers:
                - name: NodeAppContainer
                  imagePullPolicy: Never
                  image: docker.io/kundankkkrishna/nodeapp:latest
                  resources:
                      limits:
                          cpu: 50m
                          memory: 100Mi
                      requests:
                          cpu: 25m
                          memory: 10Mi
                  ports:
                      - containerPort: 8080
                      
---
apiVersion: v1
kind: Service
metadata:
  name: NodeService
  labels:
    app: nodeapp
spec:
  type: LoadBalancer
  ports:
    - port: 8081
      targetPort: 8081
  selector:
    app: nodeapp
